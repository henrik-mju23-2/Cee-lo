<Page
    x:Class="Cee_lo.StartPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cee_lo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Height="720" Width="1280" Background="#FF999999">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="0*"/>
            <ColumnDefinition Width="0*"/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="TitleText" HorizontalAlignment="Left" Margin="195,117,0,0" TextWrapping="Wrap" Text="Cee-lo" VerticalAlignment="Top" FontSize="300" RenderTransformOrigin="0.5,0.5" FocusVisualSecondaryBrush="#99000000" SelectionChanged="TitleText_SelectionChanged" Height="399" Width="885">
            <TextBlock.RenderTransform>
                <CompositeTransform TranslateX="2"/>
            </TextBlock.RenderTransform>
        </TextBlock>
        <Button x:Name="StartButton" Content="Start" Margin="0,516,0,0" VerticalAlignment="Top" Background="#FF0062FF" Width="175" Height="75" FontSize="48" CornerRadius="15,15,15,15" BorderThickness="1,1,1,1" Click="StartButton_Click" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Center"/>
        <Button x:Name="ExitButton" Margin="1202,27,0,0" VerticalAlignment="Top" Width="50" Height="50" Style="{StaticResource AccentButtonStyle}" Click="ExitButton_Click">
            <Button.Background>
                <ImageBrush ImageSource="/Assets/ExitButton.png"/>
            </Button.Background>
        </Button>
    </Grid>
</Page>